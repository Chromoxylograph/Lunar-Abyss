package uitest.java;

import mindustry.world.Tiles;

public abstract class MenuBackground {
    /** Generates a world displayed in the menu. */
    void generateWorld(int width, int height) {}

    /** Renders the menu background. */
    void render() {}
}
